{
  "servers": {
    "unity-cli": {
      "command": "dotnet",
      "args": [
        "run",
        "--file",
        "${workspaceFolder}/.unity-cli/Launcher.cs",
        "--",
        "--project",
        "${workspaceFolder}/src/UnityCliRunner.Unity3d"
      ]
    }
  }
}