{
  "mcpServers": {
    "unity-cli": {
      "command": "dotnet",
      "args": [
        "run",
        "--file",
        "${CLAUDE_PROJECT_DIR:-.}/.unity-cli/Launcher.cs",
        "--",
        "--project",
        "${CLAUDE_PROJECT_DIR:-.}/src/UnityCliRunner.Unity3d"
      ]
    }
  }
}